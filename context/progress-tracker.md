# Progress Tracker

Update this file whenever the current phase, active feature, or implementation state changes.

## Current Phase

- Backend & AI Workflows

## Current Goal

- Implement backend flow for design generation (Unit 22: Design Agent API).

## Completed

- `[x]` 01-design-system — shadcn/ui initialized, all 7 UI primitive components added, `lib/utils.ts` `cn()` helper in place, dark-only `globals.css` updated with full Ghost AI design token palette.
- `[x]` 02-editor-chrome — `EditorNavbar` (fixed top bar, sidebar toggle with `PanelLeftOpen`/`PanelLeftClose`) and `ProjectSidebar` (floating overlay, slide-in, `Tabs` for My Projects / Shared, empty placeholders, New Project button) created in `components/editor/`. Dialog pattern deferred to future use as specified.
- `[x]` Editor wiring — `EditorShell` manages sidebar state and wires `EditorNavbar` (receives `isSidebarOpen` and `onSidebarToggle` props) with `ProjectSidebar` (receives `isOpen` and `onClose` props).
- `[x]` 03-auth — `ClerkProvider` wraps root layout with dark theme + CSS variable overrides. `proxy.ts` at project root (protected-first: everything blocked except `/sign-in(.*)` and `/sign-up(.*)`). Sign-in and sign-up pages at `app/(auth)/sign-in/[[...sign-in]]/` and `app/(auth)/sign-up/[[...sign-up]]/` — two-panel layout on large screens (branding left, Clerk form right), form-only on mobile, no hardcoded colors. `/` redirects authenticated users to `/editor`, unauthenticated to `/sign-in`. Editor moved to `app/editor/page.tsx`. `UserButton` added to `EditorNavbar` right section. `@clerk/ui` installed. `NEXT_PUBLIC_CLERK_SIGN_IN_URL` and `NEXT_PUBLIC_CLERK_SIGN_UP_URL` added to `.env.local`.
- `[x]` 04-project-dialogs — Editor home screen (`EditorHome`) added with heading and `New Project` button. `useProjectDialogs` hook in `hooks/use-project-dialogs.ts` manages dialog/form/loading state and mock project data. Three dialogs in `components/editor/project-dialogs.tsx`: Create (live slug preview), Rename (auto-focus, Enter submits, prefilled), Delete (destructive confirm). `ProjectSidebar` updated with per-project rename/delete actions (hover-reveal, owned projects only), mock data list, and mobile backdrop scrim. `ProjectDialogsContext` in `contexts/project-dialogs-context.tsx` lets `EditorHome` call `openCreate` without prop-drilling. All state owned in `EditorShell`. No API calls or persistence.
- `[x]` 05-prisma — `prisma/models/project.prisma` with `Project` and `ProjectCollaborator` models (cascade delete, indexes on `ownerId`, `createdAt`, `email`, `projectId/createdAt`; unique on `projectId/email`). `lib/prisma.ts` cached singleton branches on `DATABASE_URL` prefix: `prisma+postgres://` → `@prisma/adapter-pg` + `withAccelerate()` extension; otherwise direct `@prisma/adapter-pg`. Global cache in dev for hot-reload safety. Migration `20260711095929_init_projects` applied. Prisma Client (v7.8.0) generated to `app/generated/prisma/`. `tsc --noEmit` passes with zero errors.
- `[x]` 06-project-apis — Four REST endpoints under `app/api/projects/`: `GET /api/projects` lists the authenticated user's projects (ordered by `createdAt` desc); `POST /api/projects` creates a project defaulting name to `Untitled Project`; `PATCH /api/projects/[projectId]` renames (owner-only); `DELETE /api/projects/[projectId]` deletes (owner-only, returns 204). Unauthenticated requests return 401; non-owner mutations return 403. Uses Clerk `auth()` from `@clerk/nextjs/server`. `npm run build` passes — both routes appear as dynamic (ƒ) in the route table.
- `[x]` 07-wire-editor-home — `lib/data/projects.ts` server-side helper fetches owned and shared projects. `hooks/use-project-actions.ts` replaces mock hook with real `fetch` calls: create POSTs and navigates to new workspace, rename PATCHes and refreshes, delete DELETEs and redirects to `/editor` if the active project was deleted, otherwise refreshes. `app/editor/page.tsx` converted to async Server Component passing `ownedProjects`/`sharedProjects` to `EditorShell`. `EditorShell` accepts these props plus optional `activeProjectId`. `ProjectSidebar` updated to use `ProjectRow` type. `CreateProjectDialog` accepts `roomIdPreview` prop (computed in hook with unique suffix). `npm run build` passes.
- `[x]` 08-editor-workspace-shell — Implemented workspace shell at `/editor/[roomId]`. Created `lib/project-access.ts` to check ownership and collaborator access based on Clerk `auth()` and `currentUser()?.primaryEmailAddress`. Added `<AccessDenied />` for unauthenticated or unauthorized users. Implemented docked side-by-side bento layout for Projects sidebar (with clickable project items navigating to `/editor/[roomId]`), central Canvas with custom grid background and centered Compass icon placeholder, and right AI Copilot sidebar toggled from the navbar. Designed two-line Project Header and actions in `EditorNavbar`. `tsc --noEmit` and `npm run build` pass successfully.
- `[x]` 09-share-dialog — Implemented workspace sharing: created `GET`, `POST`, and `DELETE` handlers under `/api/projects/[projectId]/collaborators` with ownership and access enforcement; integrated Clerk backend API (`createClerkClient`) to batch enrich collaborator emails with Clerk display names and avatar images; built `<ShareDialog />` utilizing `@/components/ui/dialog` with invite form, collaborator management list (with initials fallback), and one-click URL sharing copy feedback, custom-styled to match the provided UI design layout exactly. `tsc` and `npm run build` pass.
- `[x]` Navbar sidebar toggle — Enabled projects sidebar toggle (`PanelLeftOpen`/`PanelLeftClose`) and interactive Ghost logo click functionality on the `/editor` home dashboard page (when no active project is loaded / `projectName` is undefined), matching the visual behaviour of the editor workspace.
- `[x]` 10-liveblocks-setup — Configured global TypeScript typings in `liveblocks.config.ts` for Presence (`cursor`, `isThinking`) and UserMeta (`id`, `info` with name, avatar, and color). Added a cached server-side Liveblocks node client in `lib/liveblocks.ts` with a deterministic hash-to-color helper using the UI design system's palette. Implemented the authenticated `POST /api/liveblocks-auth` route handler protecting room entry using Clerk, verifying project permissions via `checkProjectAccess`, ensuring the Liveblocks room exists using `getOrCreateRoom`, and returning a signed session token.
- `[x]` 11-base-canvas — Replaced static workspace shell canvas placeholder with a live, collaborative React Flow canvas. Setup `CanvasWrapper` with `LiveblocksProvider`, `RoomProvider`, `ClientSideSuspense` (displaying a custom dark glowing loading skeleton), and `ErrorBoundary` (gracefully handling connection errors). Wired the canvas using `useLiveblocksFlow` with empty initial nodes and edges, loose connection mode, fitView, custom styled `<MiniMap>`, dot background grid, and `<Cursors />` overlay tracking collaborator mouse movements. Defined TypeScript contracts in `types/canvas.ts`.
- `[x]` 12-shape-panel — Implemented a floating shapes panel toolbar containing draggable shape buttons (rectangle, diamond, circle, pill, cylinder, hexagon) at the bottom center of the canvas; added drag and drop coordinates translation using React Flow's `screenToFlowPosition` and centering calculations on drop; registered custom `canvasNode` component type to render dropped nodes as bordered rectangles with hover-reveal connection handles and dynamic theme background/text color pairing.
- `[x]` 13-node-shape & Canvas Visual Issues — Implemented full shape rendering with CSS/SVG representations for all 6 shapes (rectangle, circle, pill, diamond, cylinder, hexagon); integrated xyflow's NodeResizer inside CanvasNodeComponent; refactored the layout to remove the outer container floating cards, making the canvas occupy 100% of the screen under the navbar; changed sidebars (left & right) to float as overlays with backdrop blur and shadows; added a custom HTML5 ghost drag preview outline for all shapes matching their drop dimensions. Verified builds and runs successfully.
- `[x]` 14-node-editing — Integrated inline label editing into CanvasNodeComponent. Added double-click to edit, auto-focusing/selecting textarea, auto-grow vertical height behavior, escape-to-cancel, enter-to-confirm, and drag/pan blocking using `nodrag nowheel` style boundaries. Verified full collaborative state propagation and compiled successfully.
- `[x]` 15-node-color-toolbar — Implemented a floating color toolbar above selected nodes. Rendered 8 predefined color swatches with hover glows, active indicators, and click handlers that immediately update the node's background and text colors collaboratively, with pointer event interception to prevent canvas dragging.
- `[x]` 16-edge-behavior — Implemented CanvasEdgeComponent with getSmoothStepPath right-angle routing, rounded corners, default MarkerType.ArrowClosed arrowheads, thick invisible hover overlay pathways, and highlight/glow visual changes. Created double-click inline edge label editor with layout-shift-free auto-growing CSS text input, blur/enter/escape auto-save updates, nodrag nowheel boundary blocks, and Liveblocks Room integration. Updated types/canvas.ts with CanvasEdge EdgeData properties.
- `[x]` 17-canvas-ergonomics — Added bottom-left floating control bar featuring animated zoom in/out/fit controls and Liveblocks collaborative undo/redo state buttons. Implemented custom `hooks/useKeyboardShortcuts.ts` to capture canvas shortcuts (+, -, Ctrl+Z, Ctrl+Y, Ctrl+Shift+Z), configured to ignore triggering keys when input, textarea, or contentEditable fields are active.
- `[x]` 18-starter-templates — Designed three templates (Microservices, CI/CD pipeline, Event-driven) using correct shapes and colors. Created `<StarterTemplatesModal>` and visual `<TemplatePreview>` wrapper computing node bounds to scale and center previews. Integrated a navbar "Templates" button dispatching CustomEvents to trigger clear-and-draw state imports and animated fitViews collaboratively.
- `[x]` 19-presence-avatars-cursors.md — Implemented collaborative presence avatars and live cursors. Elevated Liveblocks `RoomProvider` to `app/editor/[roomId]/page.tsx` so the navbar could access room state. Created `<CollaboratorAvatars>` in the navbar right-section (conditionally rendered in rooms) mapping over `useOthers` to show a styled overlapping stack of up to 5 profile photos/initials (excluding the local user ID) with a `+N` overflow badge and vertical divider. Created `<LiveCursors>` mapping `presence.cursor` to render colored SVG arrows and name badges on the canvas. Wired `onPointerMove`/`onPointerLeave` on the canvas wrapper to broadcast `cursor` coordinates.
- `[x]` 20-ai-sidebar-shell.md — Extracted the AI Copilot placeholder from `editor-shell.tsx` into a dedicated `<AiSidebar>` component. Maintained the existing slide-in floating behavior while implementing a tabbed interface (AI Architect and Specs) using Shadcn UI Tabs. Built out the "AI Architect" chat tab with empty state starter chips, mock chat bubbles styling (right-aligned for user, left-aligned for assistant), and an auto-resizing textarea input. Built out the "Specs" tab with a "Generate Spec" button and a static demo document card, adhering strictly to existing color tokens and design specs.
- `[x]` 21-canvas-autosave.md — Implemented autosave and persistence for collaborative canvas using Vercel Blob and Prisma. Created backend GET/PUT API routes for canvas state. Developed custom `useCanvasAutosave` hook to debounce and commit JSON canvas state. Integrated initialization logic within `BaseCanvasInner` to selectively load historical blob state upon entry into completely empty rooms, preserving any pre-existing Liveblocks collaborative session states. Added manual Save button in workspace navbar (Saving/Saved/Error temporary states) and converted storage/retrieval to use private Vercel Blob access. Implemented collaborative deletion of selected nodes and edges via Backspace/Delete shortcuts.
- `[x]` Node Delete Button — Added a node delete button with a `Trash2` dustbin icon next to the color swatches on the selected node's toolbar, enabling quick collaborative element deletion via `deleteElements` from `useReactFlow`.
- `[x]` Collaborative Cursors ViewportPortal — Wrapped collaborative cursors inside `<ViewportPortal>` so they translate and scale correctly with zoom and pan in the viewport.
- `[x]` Provider Reordering — Repositioned the error boundary `CanvasWrapper` to wrap the outer `RoomProviderWrapper` so that any Liveblocks connection or authentication errors are caught by `CanvasErrorFallback` instead of escaping and crashing the workspace.
- `[x]` AI Sidebar Keyboard Navigation and Focus — Wrapped the closed `AiSidebar` in an `inert` attribute to exclude its hidden DOM subtree from tab focus, and set `onClose` to restore focus to the AI navbar toggle button.

## In Progress
 
- `[/]` 22-design-agent-api — Setting up backend Trigger.dev integration, database schema for `TaskRun` tracking, and API endpoints for design triggers and scoped public token generation.
 
## Next Up

- 23-design-agent-logic.md (or next spec)

## Open Questions

- Add unresolved product or implementation questions here.

## Architecture Decisions

- Prisma (05): Schema split into `prisma/schema.prisma` (generator + datasource) and `prisma/models/project.prisma` (models), picked up automatically by `prisma.config.ts` `schema: "prisma/"`. Generator uses `provider = "prisma-client"` (v7) with explicit `output = "../app/generated/prisma"`. `PrismaClient` requires an adapter in v7 — both the direct and Accelerate branches pass `new PrismaPg({ connectionString: url })`. The Accelerate branch additionally chains `.$extends(withAccelerate())`. `@prisma/extension-accelerate` is installed but the Accelerate branch is only active when `DATABASE_URL` starts with `prisma+postgres://`.
- Project APIs (06): Route handlers use Next.js 16 `params: Promise<{ projectId: string }>` (dynamic params are now Promises). Auth uses `const { userId } = await auth()` from `@clerk/nextjs/server` — not `auth.protect()` which returns 404 for unauthenticated API requests instead of 401. Owner enforcement is done by fetching only `{ id, ownerId }` before any mutation, comparing to `userId`, and returning 403 if they differ. DELETE returns `new Response(null, { status: 204 })` with no body.
- Wire editor home (07): `app/editor/page.tsx` is an async Server Component — no `"use client"`. Data is fetched with `getUserProjects()` from `lib/data/projects.ts` using Prisma + Clerk `auth()`. The Prisma return type is inferred with `Awaited<ReturnType<typeof prisma.project.findFirst>>` to avoid importing from generated client paths that differ from the barrel export. `useProjectActions` generates a unique room ID suffix with `Math.random().toString(36).slice(2,7)` concatenated after the slug. The sidebar now receives separate `ownedProjects` and `sharedProjects` arrays instead of a mixed `projects` array filtered internally.
- Liveblocks Setup (10): Declared typed interfaces for global `Liveblocks` state in `liveblocks.config.ts`. Cached the server-side `@liveblocks/node` client on `globalThis` during development to survive hot module reloads, similar to the Prisma client singleton pattern. Implemented deterministic hashing on user IDs in `getCursorColor` to consistently map collaborators to a specific color from our 9-color design palette. Secured the `/api/liveblocks-auth` route by requiring Clerk sessions and delegating authorization checks to the existing project-level `checkProjectAccess` helper before invoking `liveblocks.identifyUser`.
- Base Canvas (11): Spliced the client-side Liveblocks wrappers and the React Flow canvas to render collaboratively on `/editor/[roomId]` workspace pages. Structured types in `types/canvas.ts` mapping to `@xyflow/react` interfaces for custom node/edge contracts. Used global CSS imports for React Flow and Liveblocks component UI styles in the client canvas module to avoid polluting root layout scope.

## Session Notes

- Wiring lives in `EditorShell` (client component): owns `sidebarOpen` state and `toggleButtonRef`. `EditorNavbar` receives `isSidebarOpen`, `onSidebarToggle`, and `toggleRef` (forwarded to the toggle `<Button>`). `ProjectSidebar` receives `isOpen` and `onClose`; closing calls `toggleButtonRef.current?.focus()` to return keyboard focus to the navbar toggle. `inert` on the closed sidebar prevents keyboard traversal into off-screen content.
- Auth (03): `ClerkProvider` lives above `<html>` in `app/layout.tsx`. Route protection uses protected-first `clerkMiddleware` in `proxy.ts` (Next.js 16 filename convention). Auth pages live in a `(auth)` route group to isolate them from the editor layout. Editor route is now at `/editor` (previously `/`).
- Project Dialogs (04): Dialog state lives exclusively in `EditorShell` via `useProjectDialogs`. `ProjectDialogsContext` is provided by `EditorShell` so child components (`EditorHome`) can open dialogs without props. `EditorPage` remains a Server Component (no `"use client"`) to preserve `export const metadata`. `MockProject.isOwned` controls whether rename/delete actions are shown in the sidebar item row.
- Issues (current-issues.md): Two bugs fixed. (1) `project-sidebar.tsx` — added `group-focus-within:opacity-100` alongside `group-hover:opacity-100` on the action container `div` so keyboard focus on either Rename or Delete reveals the buttons. (2) `hooks/use-project-dialogs.ts` — `pendingTimerRef` (useRef) now tracks every `setTimeout` handle; `closeDialog` cancels and nullifies it before resetting state; each submit callback nullifies the ref at the start of its body so an already-dismissed dialog cannot apply stale state updates.
- pg SSL deprecation warning (07 follow-up): After wiring real DB calls in `EditorPage`, a Node.js `Warning` surfaced in the Next.js overlay on every `/editor` load: `SECURITY WARNING: The SSL modes 'prefer', 'require', and 'verify-ca' are treated as aliases for 'verify-full'`. Root cause — `DATABASE_URL` contained `?sslmode=require`; the `pg-connection-string` library (used internally by `@prisma/adapter-pg` → `pg`) warns that `require`/`prefer`/`verify-ca` will change semantics in `pg v9.0.0`. Fix: changed `sslmode=require` → `sslmode=verify-full` in both `.env` and `.env.local`. Security level is identical; the new value is the explicit canonical name for the current behaviour, which silences the warning.
- Sidebar Toggle Fix: Restored the Projects sidebar toggle button (`PanelLeftOpen`/`PanelLeftClose`) and logo click functionality on the `/editor` home dashboard page by adjusting conditional checks in `components/editor/editor-navbar.tsx` so they are not restricted to pages where `projectName` is loaded.
- Collaborator Access Bug: Fixed `lib/data/projects.ts` lookup logic which queried `projectCollaborator` records matching `email: userId` instead of the user's actual email address. Corrected it to fetch `currentUser()` and query by their email address.
- Liveblocks Cache Invalidation: Fixed an HMR caching issue in `lib/liveblocks.ts` where updating `LIVEBLOCKS_SECRET_KEY` in `.env.local` did not update the client instance because it was cached indefinitely on `globalThis`. The cache helper now tracks `liveblocksKey` and recreates the `Liveblocks` client instance if the key in the environment variable changes.
- Infinite Canvas & Floating Sidebars: Modified `EditorShell` to position the main canvas `absolute inset-x-0 bottom-0 top-14 z-0` to fill the viewport. Refactored both the project left sidebar and AI Copilot right sidebar to render as overlays with `fixed` positioning, `bg-bg-surface/90 backdrop-blur-md shadow-2xl`, and translation-based toggling (`-translate-x-[calc(100%+24px)]` and `translate-x-[calc(100%+24px)]` respectively) to eliminate border/shadow peeking when closed.
- Custom Node Shapes & Resizer: Updated `CanvasNodeComponent` to render rectangles, circles, and pills using Tailwind border radius classes, and diamonds, cylinders, and hexagons using SVGs with `vector-effect="non-scaling-stroke"`. Rendered React Flow's `<NodeResizer>` controls inside `CanvasNodeComponent` (visible on selection) to allow users to scale the shapes collaboratively.
- HTML5 Drag Preview: Added custom HTML5 ghost drag image generation in `onDragStart` that dynamically constructs an off-screen element with a dashed outline matching the shape and dimensions to be dropped on the canvas.
- Node Shape Verification (13): Verified the node shape rendering implementation against the specification (rectangle, pill, and circle use CSS styling; diamond, hexagon, and cylinder use SVG shapes with scaling and vector-effect; borders are subtle at rest and highlight when selected; dragging triggers a matching ghost preview). Verified that `npm run build` completes successfully with no TypeScript type errors.
- Inline Label Editing (14): Implemented inline label editing within `CanvasNodeComponent` using a vertically centered, auto-growing `<textarea>` controlled by local `isEditing` state. Bounded interactions with `nodrag nowheel` to block canvas panning/dragging while typing. Enabled Escape to exit/cancel and Enter (without Shift) to submit. Wired text changes to standard React Flow hooks (`setNodes`), automatically syncing with Liveblocks collaborative session.
- React Flow Handle Error Fix: Resolved a browser console error (`Couldn't create edge for source handle id`) under `ConnectionMode.Loose`. The issue was caused by React Flow expecting handle elements on edge source nodes to strictly have `type="source"`. By updating all four connection handles (`top`, `bottom`, `left`, `right`) on custom nodes to `type="source"`, React Flow successfully maps handles on source nodes. In `ConnectionMode.Loose`, they still accept target connections seamlessly, resolving error #008.
- Node Color Toolbar (15): Implemented a floating color toolbar using React Flow's `<NodeToolbar>` component, visible on node selection. Rendered the 8 predefined theme colors as circular swatches, with an active ring indicator, text-colored inner dot, and dynamic hover glows (50% opacity shadow-glow overlay). Handled `onPointerDown` and `onClick` with event propagation block along with `nodrag nowheel` boundaries to prevent canvas drag/pan interference. Updating swatches triggers immediate theme updates collaboratively via `setNodes` hooks.
- Glowing Node Shapes (15-followup): Refactored CSS and SVG node shapes to use custom-colored borders and glowing shadows based on their active accent/text color. Set borders to transition from 20% opacity at rest to 50% opacity on hover and 100% when selected. Integrated outer and inset glowing box-shadows (CSS shapes) and drop-shadow filters (SVG shapes) using local CSS variables.
- Custom Edge Behavior (16): Created and registered `<CanvasEdgeComponent>` rendering clean rounded step lines (`getSmoothStepPath` with `borderRadius: 8`) and closed arrowheads (`MarkerType.ArrowClosed`). Added a thick invisible overlay path to optimize click/hover targets. Implemented inline label badge editing using double-click toggles, CSS-based auto-growing text inputs, and automatic saving on blur/Enter/Escape via `setEdges` mutations, protected with `nodrag nowheel` boundaries to isolate canvas interactions.
- Canvas Ergonomics & Shortcuts (17): Integrated bottom-left floating control bar featuring animated zoom (out/fit/in) and history (undo/redo) buttons linked to Liveblocks Room storage. Created `hooks/useKeyboardShortcuts.ts` listener capturing window keydowns (zoom mapping to +/- and history mapping to Ctrl+Z, Ctrl+Y, Ctrl+Shift+Z), verifying it ignores triggers when typing inside inputs, textareas, or contentEditable fields.
- Starter Templates Library (18): Designed three starter template data structures (Microservices, CI/CD Pipeline, Event-Driven System) using correct node shape and color settings, refactored to match exact screenshot layout configurations (fanned architectures and stacks). Created `<StarterTemplatesModal>` and `<TemplatePreview>` to dynamically render viewport-scaled center-aligned node diagrams using a custom responsive percentage coordinate mapper connecting handles, styled with clean border boxes, no text labels, and solid-opacity color fill blocks. Styled the modal as a wide dialog (`max-w-6xl w-[95vw]`) with import action buttons, kbd shortcuts, and proper description text matching the specs.
- Clerk Turbopack JS Loading Error: Fixed "Failed to load Clerk JS" issue caused by Turbopack omitting `NEXT_PUBLIC_` env vars from the client bundle when they aren't explicitly referenced in user source code. Passed `process.env.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY`, `signInUrl`, and `signUpUrl` explicitly to `<ClerkProvider>` in `app/layout.tsx` to force Turbopack injection.
- Canvas Autosave Error: Fixed "Failed to save" backend error (`500`) when uploading canvas JSON data via Vercel Blob. The `@vercel/blob` SDK's implicit `BLOB_READ_WRITE_TOKEN` pickup failed under Next.js Turbopack for the API route. Resolved by passing `token: process.env.BLOB_READ_WRITE_TOKEN` explicitly into the `put()` configuration options in `app/api/projects/[projectId]/canvas/route.ts`.
- Next.js 16 Proxy Convention Renaming: Resolved the runtime error `Could not parse module '[project]/proxy.ts', file not found` by renaming `middleware.ts` to `proxy.ts` at the root directory of the project. This also resolved the client-side `Failed to save` error on the canvas, which was triggered by the failing middleware block.
- Manual Workspace Save & Deletion Shortcuts: Integrated a manual Save button in the workspace navbar that clears pending debounce timers and triggers immediate save actions. Updated canvas API routes to store/retrieve private blobs. Added Backspace/Delete key listeners to perform collaborative element deletions on selected canvas items, checking `document.activeElement` to avoid interfering with inline label editing.
- Canvas and UI Issues (current-issues.md): Solved all remaining issues. (1) Deletion - moved Delete/Backspace key listener to React Flow wrapper keydown handler, disabled default react flow key deletion with `deleteKeyCode={null}`, and cleaned up duplicate handlers. (2) Handles - rendered both target and source handles at all positions (top, bottom, left, right) to allow collaborative connection between any two handles. (3) Drag & Drop - captured client offset inside the drag button on start, manually projected coordinates in drop using container rect and pan/zoom offsets to align nodes centered under cursor. (4) Auto-zoom - disabled automatic fitView on dropping the first node on empty canvas using a ref-based check. (5) Clerk - added img.clerk.com as allowed remote pattern in next.config.ts. (6) UserButton - conditionally hid the Clerk UserButton in the workspace navbar when projectName is defined.
